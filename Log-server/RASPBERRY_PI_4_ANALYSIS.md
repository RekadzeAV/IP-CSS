# Анализ создания сервера сбора, хранения и анализа логов на Raspberry Pi 4

**Дата создания:** Январь 2025
**Версия:** 1.0
**Целевая платформа:** Raspberry Pi 4 (4GB RAM) с Raspberry OS Full x64

> **📚 Полный индекс документации:** [DOCUMENTATION_INDEX.md](../DOCUMENTATION_INDEX.md)

## Содержание

1. [Обзор](#обзор)
2. [Анализ возможностей Raspberry Pi 4](#анализ-возможностей-raspberry-pi-4)
3. [Требования к серверу логов](#требования-к-серверу-логов)
4. [Сравнение технологий](#сравнение-технологий)
5. [Рекомендуемая архитектура](#рекомендуемая-архитектура)
6. [Оценка производительности](#оценка-производительности)
7. [План реализации](#план-реализации)
8. [Риски и ограничения](#риски-и-ограничения)
9. [Альтернативные решения](#альтернативные-решения)

---

## Обзор

### Цель анализа

Провести комплексный анализ возможности создания централизованного сервера сбора, хранения и анализа логов для системы IP-CSS на базе Raspberry Pi 4 с 4GB оперативной памяти под управлением Raspberry OS Full x64.

### Текущее состояние логирования в IP-CSS

**Существующая инфраструктура:**
- ✅ Kotlin Logging (mu.KotlinLogging) с logback-classic
- ✅ Структурированное логирование в файлы
- ✅ Логирование событий безопасности (планируется)
- ❌ Централизованный сбор логов
- ❌ Анализ и визуализация логов
- ❌ Алертинг на основе логов

**Источники логов:**
- REST API сервер (Ktor) - `server/api/`
- Веб-интерфейс (Next.js) - `server/web/`
- Android клиенты
- iOS клиенты
- Desktop клиенты
- Нативные библиотеки (C++)

---

## Анализ возможностей Raspberry Pi 4

### Технические характеристики

| Параметр | Значение |
|----------|----------|
| **Процессор** | Broadcom BCM2711, Quad-core Cortex-A72 (ARMv8) 64-bit @ 1.8GHz |
| **Память** | 4GB LPDDR4-3200 SDRAM |
| **Хранилище** | MicroSD (рекомендуется 64GB+ Class 10) или USB SSD |
| **Сеть** | Gigabit Ethernet, Wi-Fi 5 (802.11ac), Bluetooth 5.0 |
| **USB** | 2x USB 3.0, 2x USB 2.0 |
| **Видео** | VideoCore VI GPU (не используется для логирования) |
| **Питание** | 5V/3A через USB-C |

### Ограничения и возможности

#### ✅ Преимущества

1. **Низкое энергопотребление**
   - ~5-7W в режиме простоя
   - ~10-15W под нагрузкой
   - Идеально для 24/7 работы

2. **Достаточная производительность для логирования**
   - 4 ядра ARM Cortex-A72 достаточно для обработки логов
   - 4GB RAM позволяет запускать легковесные стеки

3. **Низкая стоимость**
   - Raspberry Pi 4 4GB: ~$55-75
   - Минимальные затраты на инфраструктуру

4. **Компактность**
   - Размеры: 88mm x 58mm x 19.5mm
   - Легко разместить в любом месте

5. **Активное сообщество и поддержка**
   - Большое количество документации
   - Готовые образы Docker для ARM64

#### ⚠️ Ограничения

1. **Ограниченная память (4GB)**
   - Не подходит для тяжелых стеков (полный ELK)
   - Требуется оптимизация и выбор легковесных решений

2. **Производительность CPU**
   - ARM архитектура медленнее x86-64
   - Ограниченная пропускная способность для индексации

3. **I/O производительность**
   - MicroSD карты имеют ограниченную скорость записи
   - Рекомендуется USB SSD для лучшей производительности

4. **Отсутствие ECC памяти**
   - Риск потери данных при сбоях памяти
   - Требуется регулярное резервное копирование

5. **Тепловыделение**
   - При высокой нагрузке требуется активное охлаждение
   - Рекомендуется радиатор или вентилятор

### Рекомендации по конфигурации

**Минимальная конфигурация:**
- Raspberry Pi 4 4GB
- MicroSD 64GB Class 10 (или лучше)
- Блок питания 5V/3A
- Пассивное охлаждение (радиатор)

**Рекомендуемая конфигурация:**
- Raspberry Pi 4 4GB
- USB 3.0 SSD 128GB+ (для лучшей производительности)
- Блок питания 5V/3A
- Активное охлаждение (вентилятор)
- Корпус с вентиляцией

**Оптимальная конфигурация:**
- Raspberry Pi 4 4GB
- USB 3.0 SSD 256GB+ NVMe через адаптер
- Блок питания 5V/3A
- Активное охлаждение
- UPS для защиты от отключений питания

---

## Требования к серверу логов

### Функциональные требования

1. **Сбор логов**
   - Прием логов от всех компонентов IP-CSS
   - Поддержка различных форматов (JSON, plain text, syslog)
   - Буферизация при высокой нагрузке
   - Надежная доставка (at-least-once или exactly-once)

2. **Хранение логов**
   - Хранение минимум 30 дней логов
   - Сжатие старых логов
   - Ротация логов
   - Индексация для быстрого поиска

3. **Анализ логов**
   - Поиск по тексту и полям
   - Фильтрация по уровням, источникам, времени
   - Агрегация и статистика
   - Выявление паттернов и аномалий

4. **Визуализация**
   - Дашборды с графиками
   - Временные ряды событий
   - Статистика по источникам
   - Топ ошибок и предупреждений

5. **Алертинг**
   - Уведомления о критических ошибках
   - Пороговые значения для метрик
   - Email/Telegram/Slack интеграция

### Нефункциональные требования

1. **Производительность**
   - Обработка до 10,000 логов/сек (пиковая нагрузка)
   - Задержка индексации < 5 секунд
   - Время поиска < 1 секунды для типичных запросов

2. **Надежность**
   - Доступность 99.5% (4.38 часа простоя в месяц)
   - Защита от потери данных
   - Автоматическое восстановление после сбоев

3. **Масштабируемость**
   - Возможность расширения до 50,000 логов/сек
   - Горизонтальное масштабирование (не критично для Pi)

4. **Безопасность**
   - Шифрование передачи логов (TLS)
   - Аутентификация источников логов
   - Защита от переполнения (rate limiting)

5. **Ресурсы**
   - Использование памяти < 2GB
   - Использование CPU < 50% в среднем
   - Дисковое пространство: расчет ниже

### Расчет дискового пространства

**Предположения:**
- Средний размер лога: 500 байт
- Объем логов: 5,000 логов/сек в среднем
- Пиковая нагрузка: 10,000 логов/сек
- Retention: 30 дней

**Расчет:**
```
Средний объем в день:
5,000 логов/сек × 86,400 сек/день × 500 байт = 216 GB/день

С учетом сжатия (gzip, ~10:1):
216 GB / 10 = 21.6 GB/день

За 30 дней:
21.6 GB × 30 = 648 GB

С учетом индексов и метаданных (+30%):
648 GB × 1.3 = 842 GB
```

**Вывод:** Требуется минимум 1TB дискового пространства для 30 дней хранения при указанной нагрузке.

**Оптимизация:**
- Уменьшение retention до 14 дней: ~400GB
- Более агрессивное сжатие: ~300GB
- Удаление неважных логов: ~200GB

---

## Сравнение технологий

### Вариант 1: ELK Stack (Elasticsearch, Logstash, Kibana)

#### Плюсы
- ✅ Полнофункциональное решение
- ✅ Мощные возможности поиска и анализа
- ✅ Богатая экосистема плагинов
- ✅ Хорошая документация

#### Минусы
- ❌ **Очень тяжелый для Raspberry Pi 4**
  - Elasticsearch требует минимум 2GB RAM только для себя
  - Logstash: ~500MB-1GB RAM
  - Kibana: ~500MB RAM
  - **Итого: ~3-4GB RAM** - не поместится в 4GB Pi
- ❌ Высокое потребление CPU
- ❌ Медленная работа на ARM

**Вердикт:** ❌ **НЕ РЕКОМЕНДУЕТСЯ** для Raspberry Pi 4 4GB

---

### Вариант 2: Loki + Promtail + Grafana

#### Плюсы
- ✅ **Легковесный стек**
  - Loki: ~200-500MB RAM
  - Promtail: ~50-100MB RAM
  - Grafana: ~100-200MB RAM
  - **Итого: ~400-800MB RAM** - отлично для Pi
- ✅ Оптимизирован для хранения логов (не индексирует содержимое)
- ✅ Интеграция с Prometheus для метрик
- ✅ Хорошая производительность на ARM
- ✅ Простая установка через Docker

#### Минусы
- ⚠️ Меньше возможностей поиска по сравнению с ELK
- ⚠️ Требует настройки меток (labels) для эффективного поиска
- ⚠️ Моложе экосистема (меньше плагинов)

**Вердикт:** ✅ **РЕКОМЕНДУЕТСЯ** как основной вариант

---

### Вариант 3: Graylog

#### Плюсы
- ✅ Хороший баланс функций и производительности
- ✅ Встроенный веб-интерфейс
- ✅ Поддержка различных форматов логов

#### Минусы
- ⚠️ Требует MongoDB и Elasticsearch (тяжелый)
- ⚠️ Минимум 2GB RAM для базовой установки
- ⚠️ Может быть на грани для Pi 4GB

**Вердикт:** ⚠️ **ВОЗМОЖНО**, но требует оптимизации

---

### Вариант 4: Fluentd + Elasticsearch (легковесный)

#### Плюсы
- ✅ Fluentd легковесный (~100MB RAM)
- ✅ Гибкая конфигурация

#### Минусы
- ❌ Все еще требует Elasticsearch (тяжелый)
- ❌ Не решает проблему с памятью

**Вердикт:** ❌ **НЕ РЕКОМЕНДУЕТСЯ**

---

### Вариант 5: Vector + ClickHouse + Grafana

#### Плюсы
- ✅ Vector очень легковесный (~50-100MB RAM)
- ✅ ClickHouse оптимизирован для аналитики
- ✅ Отличная производительность

#### Минусы
- ⚠️ ClickHouse требует настройки
- ⚠️ Меньше готовых решений
- ⚠️ Требует больше дискового пространства

**Вердикт:** ⚠️ **АЛЬТЕРНАТИВА**, но сложнее в настройке

---

### Вариант 6: Собственное решение на базе существующих компонентов

#### Компоненты
- **Сбор:** Fluent Bit (легковесный сборщик, ~10-50MB RAM)
- **Хранение:** SQLite/PostgreSQL + файловая система
- **Анализ:** Python скрипты или Kotlin сервис
- **Визуализация:** Простой веб-интерфейс или Grafana

#### Плюсы
- ✅ Полный контроль
- ✅ Минимальное использование ресурсов
- ✅ Интеграция с существующей системой

#### Минусы
- ❌ Требует разработки
- ❌ Меньше функций из коробки

**Вердикт:** ⚠️ **ВОЗМОЖНО** для простых случаев

---

## Рекомендуемая архитектура

### Выбранное решение: Loki + Promtail + Grafana

**Обоснование:**
- Оптимальный баланс функций и ресурсов
- Легковесность для Raspberry Pi 4
- Хорошая производительность на ARM
- Простота развертывания через Docker
- Активная разработка и поддержка

### Архитектурная диаграмма

```
┌─────────────────────────────────────────────────────────────┐
│                    IP-CSS Компоненты                        │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ REST API     │  │ Web UI       │  │ Android/iOS  │     │
│  │ (Ktor)       │  │ (Next.js)    │  │ Clients     │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                 │                 │              │
│         └─────────────────┼─────────────────┘              │
│                            │                                 │
│                   ┌────────▼────────┐                       │
│                   │  Log Files /    │                       │
│                   │  Syslog / HTTP  │                       │
│                   └────────┬────────┘                       │
└────────────────────────────┼────────────────────────────────┘
                             │
                             │ (logs)
                             ▼
┌─────────────────────────────────────────────────────────────┐
│              Raspberry Pi 4 - Log Server                    │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐ │
│  │              Promtail (Log Collector)                │ │
│  │  - Сбор логов из файлов                              │ │
│  │  - Парсинг и обогащение                              │ │
│  │  - Отправка в Loki                                    │ │
│  │  RAM: ~50-100MB                                       │ │
│  └───────────────────┬──────────────────────────────────┘ │
│                      │                                      │
│                      │ (HTTP/gRPC)                          │
│                      ▼                                      │
│  ┌──────────────────────────────────────────────────────┐ │
│  │              Loki (Log Storage)                       │ │
│  │  - Хранение логов (объектное хранилище)              │ │
│  │  - Индексация по меткам (labels)                     │ │
│  │  - Сжатие и retention                                 │ │
│  │  RAM: ~200-500MB                                      │ │
│  │  Disk: ~200GB-1TB (зависит от retention)             │ │
│  └───────────────────┬──────────────────────────────────┘ │
│                      │                                      │
│                      │ (queries)                            │
│                      ▼                                      │
│  ┌──────────────────────────────────────────────────────┐ │
│  │              Grafana (Visualization)                 │ │
│  │  - Дашборды и графики                                │ │
│  │  - Поиск и фильтрация                                │ │
│  │  - Алертинг                                           │ │
│  │  RAM: ~100-200MB                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐ │
│  │              Prometheus (Metrics)                    │ │
│  │  - Сбор метрик системы                               │ │
│  │  - Интеграция с Grafana                              │ │
│  │  RAM: ~50-100MB                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                              │
│  Итого RAM: ~400-900MB (в пределах 4GB)                    │
└─────────────────────────────────────────────────────────────┘
```

### Компоненты стека

#### 1. Promtail (Log Collector)

**Роль:** Сбор, парсинг и отправка логов в Loki

**Конфигурация:**
- Мониторинг лог-файлов IP-CSS
- Парсинг JSON логов
- Добавление меток (labels):
  - `job`: источник (api-server, web-ui, android-client)
  - `level`: уровень лога (info, warn, error)
  - `component`: компонент системы
  - `host`: хост-источник

**Ресурсы:**
- RAM: 50-100MB
- CPU: низкое (< 10%)
- Disk: минимальное (только конфигурация)

#### 2. Loki (Log Storage)

**Роль:** Хранение и индексация логов

**Особенности:**
- Хранит только метки в индексе (не содержимое логов)
- Сжатие логов (gzip, snappy)
- Retention политики
- Multi-tenancy (опционально)

**Конфигурация:**
```yaml
storage:
  filesystem:
    directory: /var/lib/loki/chunks
  retention:
    period: 720h  # 30 дней

limits_config:
  ingestion_rate_mb: 10
  ingestion_burst_size_mb: 20
  max_query_length: 720h
  max_query_parallelism: 32
```

**Ресурсы:**
- RAM: 200-500MB
- CPU: среднее (20-40%)
- Disk: 200GB-1TB (зависит от retention)

#### 3. Grafana (Visualization)

**Роль:** Визуализация, поиск и алертинг

**Возможности:**
- Дашборды с графиками логов
- LogQL запросы для поиска
- Алерты на основе логов
- Интеграция с Prometheus для метрик

**Ресурсы:**
- RAM: 100-200MB
- CPU: низкое-среднее (10-20%)
- Disk: минимальное

#### 4. Prometheus (Metrics, опционально)

**Роль:** Сбор метрик системы и приложений

**Интеграция:**
- Метрики Loki (количество логов, размер хранилища)
- Системные метрики (CPU, RAM, Disk)
- Метрики IP-CSS (через экспортеры)

**Ресурсы:**
- RAM: 50-100MB
- CPU: низкое (< 10%)
- Disk: ~1-5GB (зависит от retention метрик)

---

## Оценка производительности

### Пропускная способность

**Теоретическая производительность Loki на Raspberry Pi 4:**

| Метрика | Значение |
|---------|----------|
| **Максимальная скорость приема** | 5,000-10,000 логов/сек |
| **Средняя скорость приема** | 2,000-5,000 логов/сек |
| **Задержка индексации** | 1-3 секунды |
| **Время поиска (типичный запрос)** | 0.5-2 секунды |
| **Время поиска (сложный запрос)** | 2-5 секунд |

**Ограничения:**
- Скорость записи на MicroSD: ~20-50 MB/s
- Скорость записи на USB SSD: ~100-200 MB/s
- Рекомендуется USB SSD для лучшей производительности

### Использование ресурсов

**Типичная нагрузка (2,000 логов/сек):**

| Компонент | RAM | CPU | Disk I/O |
|-----------|-----|-----|---------|
| Promtail | 50MB | 5% | Низкое |
| Loki | 300MB | 25% | Среднее |
| Grafana | 150MB | 10% | Низкое |
| Prometheus | 80MB | 5% | Низкое |
| Система (OS) | 500MB | 10% | - |
| **Итого** | **~1,080MB** | **~55%** | Среднее |

**Пиковая нагрузка (10,000 логов/сек):**

| Компонент | RAM | CPU | Disk I/O |
|-----------|-----|-----|---------|
| Promtail | 100MB | 15% | Среднее |
| Loki | 500MB | 60% | Высокое |
| Grafana | 200MB | 15% | Низкое |
| Prometheus | 100MB | 10% | Низкое |
| Система (OS) | 500MB | 15% | - |
| **Итого** | **~1,400MB** | **~115%** (перегрузка) | Высокое |

**Вывод:** Система справляется с типичной нагрузкой, но при пиковых нагрузках может быть перегрузка CPU. Рекомендуется:
- Ограничение скорости приема (rate limiting)
- Буферизация на стороне клиентов
- Мониторинг производительности

---

## План реализации

### Этап 1: Подготовка Raspberry Pi 4

**Время:** 1-2 часа

1. **Установка Raspberry OS Full x64**
   ```bash
   # Скачать образ с официального сайта
   # Записать на MicroSD/USB SSD через Raspberry Pi Imager
   # Включить SSH и настроить сеть
   ```

2. **Базовая настройка системы**
   ```bash
   # Обновление системы
   sudo apt update && sudo apt upgrade -y

   # Установка Docker
   curl -fsSL https://get.docker.com -o get-docker.sh
   sudo sh get-docker.sh
   sudo usermod -aG docker $USER

   # Установка Docker Compose
   sudo apt install docker-compose -y

   # Настройка swap (опционально, для подстраховки)
   sudo dphys-swapfile swapoff
   sudo nano /etc/dphys-swapfile
   # CONF_SWAPSIZE=1024
   sudo dphys-swapfile setup
   sudo dphys-swapfile swapon
   ```

3. **Оптимизация для логирования**
   ```bash
   # Отключение ненужных сервисов
   sudo systemctl disable bluetooth
   sudo systemctl disable avahi-daemon

   # Настройка файловой системы для производительности
   # Если используется USB SSD, настроить монтирование с noatime
   ```

### Этап 2: Развертывание Loki Stack

**Время:** 2-3 часа

1. **Создание структуры директорий**
   ```bash
   mkdir -p ~/log-server/{loki,grafana,promtail,config}
   cd ~/log-server
   ```

2. **Создание docker-compose.yml**
   ```yaml
   version: '3.8'

   services:
     loki:
       image: grafana/loki:2.9.0
       container_name: loki
       ports:
         - "3100:3100"
       command: -config.file=/etc/loki/local-config.yaml
       volumes:
         - ./config/loki-config.yaml:/etc/loki/local-config.yaml
         - ./loki/data:/loki
       restart: unless-stopped
       networks:
         - log-network

     promtail:
       image: grafana/promtail:2.9.0
       container_name: promtail
       volumes:
         - ./config/promtail-config.yaml:/etc/promtail/config.yml
         - /var/log:/var/log:ro
         - /path/to/ip-css/logs:/var/log/ip-css:ro
       command: -config.file=/etc/promtail/config.yml
       restart: unless-stopped
       networks:
         - log-network
       depends_on:
         - loki

     grafana:
       image: grafana/grafana:10.2.0
       container_name: grafana
       ports:
         - "3000:3000"
       environment:
         - GF_SECURITY_ADMIN_PASSWORD=admin
         - GF_INSTALL_PLUGINS=grafana-clock-panel
       volumes:
         - ./grafana/data:/var/lib/grafana
         - ./grafana/provisioning:/etc/grafana/provisioning
       restart: unless-stopped
       networks:
         - log-network
       depends_on:
         - loki

     prometheus:
       image: prom/prometheus:latest
       container_name: prometheus
       ports:
         - "9090:9090"
       volumes:
         - ./config/prometheus.yml:/etc/prometheus/prometheus.yml
         - ./prometheus/data:/prometheus
       command:
         - '--config.file=/etc/prometheus/prometheus.yml'
         - '--storage.tsdb.path=/prometheus'
       restart: unless-stopped
       networks:
         - log-network

   networks:
     log-network:
       driver: bridge
   ```

3. **Конфигурация Loki**
   ```yaml
   # config/loki-config.yaml
   auth_enabled: false

   server:
     http_listen_port: 3100
     grpc_listen_port: 9096

   common:
     path_prefix: /loki
     storage:
       filesystem:
         chunks_directory: /loki/chunks
         rules_directory: /loki/rules
     replication_factor: 1
     ring:
       instance_addr: 127.0.0.1
       kvstore:
         store: inmemory

   schema_config:
     configs:
       - from: 2024-01-01
         store: boltdb-shipper
         object_store: filesystem
         schema: v11
         index:
           prefix: index_
           period: 24h

   storage_config:
     boltdb_shipper:
       active_index_directory: /loki/boltdb-shipper-active
       cache_location: /loki/boltdb-shipper-cache
       shared_store: filesystem
     filesystem:
       directory: /loki/chunks

   compactor:
     working_directory: /loki/boltdb-shipper-compactor
     shared_store: filesystem

   limits_config:
     ingestion_rate_mb: 10
     ingestion_burst_size_mb: 20
     max_query_length: 720h
     max_query_parallelism: 32
     retention_period: 720h  # 30 дней

   chunk_store_config:
     max_look_back_period: 720h

   table_manager:
     retention_deletes_enabled: true
     retention_period: 720h
   ```

4. **Конфигурация Promtail**
   ```yaml
   # config/promtail-config.yaml
   server:
     http_listen_port: 9080
     grpc_listen_port: 0

   positions:
     filename: /tmp/positions.yaml

   clients:
     - url: http://loki:3100/loki/api/v1/push

   scrape_configs:
     - job_name: ip-css-api
       static_configs:
         - targets:
             - localhost
           labels:
             job: ip-css-api
             component: server
             host: ${HOSTNAME}
       pipeline_stages:
         - json:
             expressions:
               level: level
               message: message
               timestamp: timestamp
         - labels:
             level:
         - timestamp:
             source: timestamp
             format: RFC3339
       file_sd_configs:
         - files:
             - /var/log/ip-css/api/*.log

     - job_name: ip-css-web
       static_configs:
         - targets:
             - localhost
           labels:
             job: ip-css-web
             component: web
             host: ${HOSTNAME}
       file_sd_configs:
         - files:
             - /var/log/ip-css/web/*.log

     - job_name: system
       static_configs:
         - targets:
             - localhost
           labels:
             job: system
             component: system
       file_sd_configs:
         - files:
             - /var/log/syslog
             - /var/log/auth.log
   ```

5. **Запуск стека**
   ```bash
   docker-compose up -d
   docker-compose logs -f
   ```

### Этап 3: Интеграция с IP-CSS

**Время:** 3-4 часа

1. **Настройка логирования в IP-CSS**

   **server/api/src/main/resources/logback.xml:**
   ```xml
   <configuration>
       <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
           <file>/var/log/ip-css/api/application.log</file>
           <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
               <fileNamePattern>/var/log/ip-css/api/application.%d{yyyy-MM-dd}.log</fileNamePattern>
               <maxHistory>30</maxHistory>
           </rollingPolicy>
           <encoder class="net.logstash.logback.encoder.LogstashEncoder">
               <customFields>{"component":"api-server","host":"${HOSTNAME}"}</customFields>
           </encoder>
       </appender>

       <root level="INFO">
           <appender-ref ref="FILE" />
       </root>
   </configuration>
   ```

2. **Настройка отправки логов через HTTP (опционально)**

   Добавить HTTP appender для прямой отправки в Loki:
   ```xml
   <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
       <http>
           <url>http://log-server:3100/loki/api/v1/push</url>
       </http>
       <format>
           <label>
               <pattern>component,host,level</pattern>
           </label>
           <message>
               <pattern>l=%level c=%logger{20} t=%thread | %msg %ex</pattern>
           </message>
       </format>
   </appender>
   ```

3. **Создание дашбордов в Grafana**
   - Дашборд "IP-CSS Overview"
   - Дашборд "Errors and Warnings"
   - Дашборд "Performance Metrics"
   - Дашборд "Security Events"

### Этап 4: Настройка алертинга

**Время:** 1-2 часа

1. **Создание правил алертинга в Grafana**
   - Критические ошибки (ERROR level)
   - Множественные неудачные попытки входа
   - Высокая нагрузка на систему
   - Недостаток дискового пространства

2. **Настройка уведомлений**
   - Email уведомления
   - Telegram бот (опционально)
   - Webhook для интеграции с другими системами

### Этап 5: Мониторинг и оптимизация

**Время:** Постоянно

1. **Мониторинг производительности**
   - Использование RAM и CPU
   - Размер хранилища логов
   - Скорость приема логов
   - Время отклика запросов

2. **Оптимизация**
   - Настройка retention политик
   - Оптимизация индексов Loki
   - Настройка rate limiting
   - Оптимизация запросов в Grafana

---

## Риски и ограничения

### Технические риски

1. **Переполнение диска**
   - **Риск:** Высокий при недостаточном мониторинге
   - **Митигация:**
     - Автоматическая очистка старых логов
     - Мониторинг свободного места
     - Алерты при < 10% свободного места

2. **Перегрузка CPU при пиковых нагрузках**
   - **Риск:** Средний
   - **Митигация:**
     - Rate limiting на стороне Loki
     - Буферизация на стороне клиентов
     - Мониторинг нагрузки

3. **Недостаток памяти**
   - **Риск:** Низкий при правильной настройке
   - **Митигация:**
     - Мониторинг использования памяти
     - Настройка лимитов для контейнеров
     - Использование swap (с осторожностью)

4. **Потеря данных при сбое**
   - **Риск:** Средний
   - **Митигация:**
     - Регулярное резервное копирование
     - Настройка репликации (если критично)
     - Мониторинг целостности данных

### Операционные риски

1. **Отказ MicroSD карты**
   - **Риск:** Высокий при интенсивной записи
   - **Митигация:**
     - Использование USB SSD вместо MicroSD
     - Регулярная замена носителя
     - Мониторинг здоровья диска

2. **Отключение питания**
   - **Риск:** Средний
   - **Митигация:**
     - Использование UPS
     - Graceful shutdown при отключении питания
     - Автоматический запуск после восстановления питания

3. **Перегрев**
   - **Риск:** Средний при высокой нагрузке
   - **Митигация:**
     - Активное охлаждение
     - Мониторинг температуры
     - Throttling при перегреве

### Ограничения производительности

1. **Максимальная скорость приема логов**
   - Ограничение: ~10,000 логов/сек (пиковая)
   - Для больших нагрузок требуется более мощное оборудование

2. **Время поиска**
   - Простые запросы: < 1 секунды
   - Сложные запросы: 2-5 секунд
   - Для более быстрого поиска требуется больше ресурсов

3. **Retention период**
   - Рекомендуется: 14-30 дней
   - Для более длительного хранения требуется больше дискового пространства

---

## Альтернативные решения

### Альтернатива 1: Облачное решение

**Варианты:**
- Grafana Cloud (Loki)
- Datadog
- Loggly
- Splunk Cloud

**Плюсы:**
- Не требует локального оборудования
- Автоматическое масштабирование
- Высокая доступность
- Профессиональная поддержка

**Минусы:**
- Постоянные затраты (подписка)
- Зависимость от интернета
- Проблемы с конфиденциальностью данных
- Задержки при передаче логов

**Когда использовать:**
- Нет возможности разместить локальный сервер
- Требуется высокая доступность
- Бюджет позволяет облачные решения

### Альтернатива 2: Гибридное решение

**Архитектура:**
- Локальный сервер на Pi для краткосрочного хранения (7 дней)
- Облачное хранилище для долгосрочного архива (30+ дней)

**Плюсы:**
- Баланс между стоимостью и функциональностью
- Быстрый доступ к недавним логам
- Долгосрочное хранение в облаке

**Минусы:**
- Сложность настройки
- Дополнительные затраты на облачное хранилище

### Альтернатива 3: Упрощенное решение

**Компоненты:**
- Fluent Bit для сбора
- SQLite для хранения
- Простой веб-интерфейс для просмотра

**Плюсы:**
- Минимальное использование ресурсов
- Простота настройки
- Низкая стоимость

**Минусы:**
- Ограниченные возможности поиска
- Нет визуализации
- Требует разработки веб-интерфейса

**Когда использовать:**
- Очень ограниченные ресурсы
- Простые требования к анализу
- Небольшой объем логов

---

## Выводы и рекомендации

### Рекомендуемое решение

**Стек:** Loki + Promtail + Grafana + Prometheus

**Обоснование:**
1. ✅ Оптимальный баланс функций и ресурсов
2. ✅ Укладывается в 4GB RAM Raspberry Pi 4
3. ✅ Хорошая производительность на ARM
4. ✅ Простота развертывания и поддержки
5. ✅ Активная разработка и сообщество

### Ключевые рекомендации

1. **Оборудование:**
   - Использовать USB SSD вместо MicroSD для лучшей производительности
   - Установить активное охлаждение
   - Рассмотреть UPS для защиты от отключений

2. **Конфигурация:**
   - Настроить retention на 14-30 дней (в зависимости от дискового пространства)
   - Включить rate limiting для защиты от перегрузки
   - Настроить мониторинг использования ресурсов

3. **Безопасность:**
   - Настроить TLS для передачи логов
   - Ограничить доступ к Grafana
   - Регулярно обновлять компоненты

4. **Мониторинг:**
   - Настроить алерты на критичные события
   - Мониторить использование дискового пространства
   - Отслеживать производительность системы

### Ожидаемые результаты

- ✅ Централизованный сбор логов со всех компонентов IP-CSS
- ✅ Хранение логов в течение 14-30 дней
- ✅ Быстрый поиск и анализ логов через Grafana
- ✅ Визуализация метрик и трендов
- ✅ Алертинг на критичные события
- ✅ Использование ресурсов в пределах возможностей Pi 4

### Следующие шаги

1. Приобрести и настроить Raspberry Pi 4
2. Развернуть Loki stack по плану реализации
3. Интегрировать с IP-CSS
4. Настроить дашборды и алерты
5. Провести нагрузочное тестирование
6. Оптимизировать конфигурацию на основе результатов

---

## Приложения

### Приложение A: Команды для быстрого старта

```bash
# Установка Docker на Raspberry Pi OS
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Клонирование конфигураций
git clone <repository>
cd log-server

# Запуск стека
docker-compose up -d

# Проверка статуса
docker-compose ps
docker-compose logs -f loki

# Доступ к Grafana
# http://<pi-ip>:3000
# Логин: admin / Пароль: admin (сменить при первом входе)
```

### Приложение B: Полезные LogQL запросы

```logql
# Все логи за последний час
{job="ip-css-api"}

# Только ошибки
{job="ip-css-api"} |= "ERROR"

# Логи по конкретному компоненту
{component="camera-service"}

# Подсчет логов по уровням
sum(count_over_time({job="ip-css-api"}[1h])) by (level)

# Топ ошибок
topk(10, sum(rate({job="ip-css-api"} |= "ERROR" [5m])) by (message))
```

### Приложение C: Мониторинг ресурсов

```bash
# Использование памяти
free -h

# Использование CPU
top

# Использование диска
df -h

# Использование ресурсов контейнерами
docker stats

# Температура CPU
vcgencmd measure_temp
```

---

**Последнее обновление:** Январь 2025
**Автор:** AI Assistant
**Версия документа:** 1.0

