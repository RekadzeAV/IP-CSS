# Блок 8: Расширенная аналитика и Блок 9: Дополнительный функционал

План реализации по [PROJECT_PROMPT.md](../../ai-agent/PROJECT_PROMPT.md). Текущее состояние и задачи.

---

## Блок 8: Расширенная аналитика

### 8.1 ANPR (распознавание номеров)

| Компонент | Статус | Описание |
|-----------|--------|----------|
| Алгоритм | ✅ Есть | `native/analytics`: `anpr_engine.cpp` / `anpr_engine.h`, OCR, конфиденс, bbox |
| Интеграция с событиями | ✅ Есть | `RecognizeLicensePlateUseCase`: при распознавании создаётся `Event` с типом `LICENSE_PLATE_RECOGNITION` |
| База номеров | ✅ Есть | `LicensePlateRepository`, `StoredLicensePlate`, SQLDelight-реализация, API `GET /api/v1/cameras/{id}/analytics/license-plates` (фильтр по номеру, по диапазону дат) |

**Доработки (по желанию):** белый/чёрный списки номеров, алерты при совпадении, поддержка нескольких стран в одном запросе.

---

### 8.2 Детекция лиц

| Компонент | Статус | Описание |
|-----------|--------|----------|
| Детекция в видеопотоке | ✅ Есть | `native/analytics`: `face_detector.cpp`; `AnalyticsService.detectFaces()`, `DetectFacesUseCase` |
| События | ✅ Есть | При детекции создаётся `Event` с типом `FACE_DETECTION` |
| Embeddings | ✅ Есть | `DetectedFace.embedding` (FloatArray), опция `includeEmbeddings` в Use Case |

**Доработки:** стабильный пайплайн детекции по RTSP/HLS в `VideoAnalyticsService`, настройка порога и зон.

---

### 8.3 Распознавание лиц (сравнение, база лиц, поиск/группировка)

| Компонент | Статус | Описание |
|-----------|--------|----------|
| Сравнение эмбеддингов | 🟡 Интерфейс | `AnalyticsService.compareFaces()` — контракт добавлен; реализация в нативном слое — задача |
| База лиц | 🟡 Интерфейс | `FaceRepository`, модель `StoredFace` (id, label, embedding, cameraId, createdAt) — добавлены; таблица БД и реализация — в миграции/след. этап |
| Поиск / группировка | 📋 План | Use Case: по эмбеддингу искать в `FaceRepository`, возвращать лучшие совпадения; группировка по `label` или по кластерам эмбеддингов |

**Задачи:**  
- Реализовать в native/analytics сравнение двух эмбеддингов (cosine similarity / L2).  
- Добавить миграцию БД для таблицы `face_gallery` (id, label, embedding_blob, camera_id, created_at).  
- Реализовать `FaceRepositoryImplSqlDelight`, API CRUD для базы лиц и поиск по лицу (POST frame → embedding → search).

---

### 8.4 Аналитические отчёты (генерация, экспорт CSV/PDF)

| Компонент | Статус | Описание |
|-----------|--------|----------|
| Сервис отчётов | 🟡 Интерфейс | `ReportService`: тип отчёта, параметры (камера, период), формат (CSV, PDF) — интерфейс добавлен |
| API | 🟡 Заглушка | `ReportRoutes`: GET/POST для генерации и скачивания отчёта (формат в query/body) |
| Реализация | 📋 План | Реализация `ReportServiceImpl`: агрегация из `EventRepository`, `RecordingRepository`, `LicensePlateRepository`; экспорт CSV (например, OpenCSV); PDF (например, iText или HTML→PDF). |

**Задачи:**  
- Реализовать `ReportServiceImpl`.  
- Определить набор отчётов (события по камере/периоду, записи, ANPR-статистика, дашборд-сводка).  
- Добавить экспорт в CSV и PDF.

---

## Блок 9: Дополнительный функционал (по PROJECT_PROMPT)

### 9.1 Облачная синхронизация

| Компонент | Статус | Описание |
|-----------|--------|----------|
| Синхронизация между устройствами | ✅ Есть | `CloudSyncService`: `syncCameras()`, `syncSettings()`, `syncEvents()`, `syncAll()`; конфликты и стратегии разрешения |
| API | ✅ Есть | `SyncRoutes`: запуск синхронизации, состояние, конфликты |
| Облачное хранилище / бэкапы | 🟡 Частично | `CloudStorageService`, `CloudStorageProvider`, `FileBasedCloudStorageProvider`; `DatabaseBackupService` — бэкапы БД. Расширить: бэкапы записей/конфигов в облако по расписанию. |

**Задачи:** интеграция с S3-совместимым/другим провайдером, настройка расписания бэкапов, восстановление из облака.

---

### 9.2 Расширенный PTZ (паттерны, tour, автотрекинг, геозоны)

| Компонент | Статус | Описание |
|-----------|--------|----------|
| Базовый PTZ | ✅ Есть | `ControlPtzUseCase`, ONVIF PTZ, `PTZControls` в UI |
| Паттерны / Tour | 🟡 Модели | Модели `PtzPattern`, `PtzTour`, `PtzGeozone` и интерфейс `PtzPatternRepository` добавлены; API и воспроизведение паттернов — в разработке |
| Автотрекинг | 📋 План | Использование детекции/трекинга объекта и выдача PTZ-команд для удержания объекта в кадре (серверный сервис или скрипт). |
| Геозоны | 🟡 Модель | `PtzGeozone` (имя, полигон, приоритет, presetId) — модель есть; привязка к событиям и правилам — в плане. |

**Задачи:** CRUD API для паттернов и туров, воспроизведение тура по расписанию или по запросу; автотрекинг как опция в настройках камеры; использование геозон в правилах аналитики.

---

### 9.3 Многопользовательский режим (группы, делегирование, права по камерам, аудит)

| Компонент | Статус | Описание |
|-----------|--------|----------|
| Роли и аутентификация | ✅ Есть | JWT, роли (VIEWER, OPERATOR, ADMIN), `requireRole` в маршрутах |
| Аудит | ✅ Есть | `AuditLogRepository`, `InMemoryAuditLogRepository`, `AuditRoutes`, `SecurityLogger` |
| Группы пользователей | 📋 План | Модель `UserGroup`, привязка пользователей к группам, права группы (набор камер, уровень доступа). |
| Делегирование | 📋 План | Временная передача прав на камеру/набор камер другому пользователю (срок, scope). |
| Права по камерам | 📋 План | Разрешения на уровне камеры (камера → список ролей/пользователей/групп с уровнем доступа). |

**Задачи:** схема БД для групп и прав по камерам; API групп и назначений; проверка прав в маршрутах (камера + пользователь/группа); расширение аудита на действия делегирования и смену прав.

---

### 9.4 Внешние интеграции (REST/WebSocket/GraphQL, умный дом, системы безопасности)

| Компонент | Статус | Описание |
|-----------|--------|----------|
| REST API | ✅ Есть | Ktor, маршруты `/api/v1/*`, JWT, документирование (OpenAPI при наличии). |
| WebSocket | ✅ Есть | `WebSocketServer`, подписки на события/потоки. |
| GraphQL | 📋 План | Опциональный GraphQL-эндпоинт (например, Ktor GraphQL) для гибких запросов. |
| Умный дом / охранные системы | 📋 План | Webhook при событиях (POST на URL пользователя); опционально MQTT, Home Assistant, интеграции с охранными панелями. |

**Задачи:** документировать REST и WebSocket для партнёров; добавить webhook в настройках уведомлений (URL, фильтр типов событий); при необходимости — GraphQL и MQTT/специфичные протоколы.

---

## Общие зависимости

- **Блок 8:** работающий пайплайн видеопотока и аналитики (RTSP/HLS → кадры → детекция/ANPR/лица), fat JAR или нативная библиотека на целевых платформах.
- **Блок 9:** стабильная аутентификация и роли (есть); для групп и прав по камерам — миграции БД и обновление middleware.

## Файлы, добавленные в рамках плана

- `shared/.../domain/model/StoredFace.kt` — модель лица в галерее.
- `shared/.../domain/repository/FaceRepository.kt` — репозиторий базы лиц.
- `shared/.../domain/service/AnalyticsService.kt` — добавлен метод `compareFaces`; реализации в jvm/android/desktop/ios (cosine similarity).
- `server/.../service/ReportService.kt` — интерфейс сервиса отчётов.
- `server/.../service/ReportServiceImpl.kt` — заглушка (возвращает UnsupportedOperationException).
- `server/.../routing/ReportRoutes.kt` — GET `/api/v1/reports?type=...&format=CSV|PDF&cameraId=...&from=...&to=...`.
- `shared/.../domain/model/PtzPattern.kt` — модели расширенного PTZ (PtzPattern, PtzTour, PtzGeozone).
- `shared/.../domain/repository/PtzPatternRepository.kt` — репозиторий паттернов/туров/геозон PTZ.
