# Отчет о примененных исправлениях

**Дата:** 28 января 2026

## Исследование проблемы

### Версия Kotlin
- Kotlin 2.0.21 (из build.gradle.kts)

### Проблема
Компилятор не видит `expect` декларации из `commonMain` при компиляции `desktopMain`, хотя все файлы на месте и правильно настроены.

## Примененные исправления

### 1. ApiClient.kt
- ✅ Удален `inline` из всех `suspend` функций
- ✅ Исправлено использование `TimeoutError`
- ✅ Исправлены типы в `HttpTimeout`
- ✅ Исправлены `ApiError` классы
- ✅ `expect` функции вынесены на уровень модуля

### 2. build.gradle.kts
- ✅ Добавлена явная зависимость `desktopMain` от `commonMain`
- ✅ Изменен порядок зависимостей (commonMain первым)

### 3. CertificatePinner и NativeAnalytics
- ✅ Перемещены actual реализации из `jvmMain` в `desktopMain`
- ✅ Удалены дубликаты из `jvmMain`

## Текущий статус

Проверка компиляции после перемещения файлов в desktopMain...

---

**Статус:** В процессе проверки
