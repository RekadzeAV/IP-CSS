# 🎯 ПЛАН ЗАВЕРШЕНИЯ ОСТАВШИХСЯ ЗАДАЧ (последовательный, без возвратов)

**Версия проекта:** 0.5.1.1-beta → цель 0.6.0-beta → v1.0.0 (Q4 2026)
**Дата:** 14.09.2026
**Ход выполнения:** Фаза 0 ✅ · Фаза 1 ✅ · Фаза 2 🟡 (2.1 AI-агент: pytest-гейт ЗЕЛЁНЫЙ 42 passed/2 skipped; e2e выделены в `-m e2e` — порт A) · Фаза 3 ✅ (server-build, :server:api:test зелёный, link-check 0 битых, секреты выведены, CI ужесточён). **Программно-выполнимая часть без внешних ресурсов ЗАВЕРШЕНА.** Остаются: Фаза 4 (полевая валидация — порт A1), Фаза 5 (релиз — после GO/NO-GO + GitHub Secrets), Фаза 6 (пост-релиз).
**Операционная база:** `PLAN_EXECUTION_MASTER.md` (этапы 0–7 ✅, 8 ⛔, 9–10 ⬜) + `REMAINING_TASKS.md` + `PLAN_TECH_DEBT_TODO.md` + `PLAN_REFACTORING.md` + `PLAN_VULNERABILITIES.md` (уязвимости зависимостей: npm-контур закрыт 14.09, dependabot + CI-гейты включены)

> **Принцип последовательности (анти-возврат):**
> 1. Каждый шаг выполняется **ровно один раз** и закрывается полностью (коммит + зелёный гейт) перед переходом к следующему.
> 2. Задачи, зависящие от внешних ресурсов (камеры/аппаратура, macOS/Xcode, Firebase, NAS/Linux, GitHub Secrets), **изолированы в «порты ожидания»**, чтобы не блокировать основной путь и не требовать повторного возврата к программной части после их появления.
> 3. Документация обновляется **внутри каждого шага** (п.4 исходного ТЗ), а не отдельным «большим» шагом — это исключает рассинхрон документации↔код.
> 4. Работа ведётся только в `main`; каждая веха фиксируется коммитом + push в `github`.

---

## Текущее фактическое состояние (актуализировано 15.09.2026)

| Область | Состояние |
|---|---|
| W0. Локальные коммиты в `main` | ️ `main` опережает `github/main`; push выполняется в рамках ревизии. **NAS-remote (`origin`) отключён 15.09.2026** — см. `archive/nas-git-deployment-2026-09-15/README.md`; для передачи кода требуется только `github` |
| W1. Этапы 0–7 мастер-плана | ✅ выполнены. Уточнено 15.09: **Этап 4 закрыт** (дубль Android устранён — `platforms/client-android` = только `README.md`), **Этап 6 частично** (npm 22→0, dependabot ✅; остаток gradle-контур) |
| W2. Пароли/ротация секретов | ✅ выполнено (п.1–4 исходного ТЗ, см. `SECRETS_ROTATION.md`); 15.09: реальный пароль NAS выведен из 16 трекаемых документов + pre-commit guard (`.githooks/`, `core.hooksPath=.githooks`) |
| W3. Тестовые сборки | ✅ WIN-MSI 288MB, APK 35.8MB, Docker `ipcss:0.5.1.1-beta` 1.46GB, server installDist |
| W4. Открытые программные задачи | 🟡 рабочих копий нет (дерево чисто); остаток — gradle-контур CVE (V4), pip-lock (V3), GitHub Secrets (5.2) |
| W5. Внешние блокеры | Камеры (ONVIF backlog), macOS/Xcode, Firebase-конфиг, NAS/Linux, GitHub Secrets — **нет доступа** |
| W6. Этап 8 (полевая валидация) | 🟡 **14.09: частично пройден** — сервер стартует (`/health`=200, DI-фиксы `0458ad4a`/`2881efc4`), **RTSP field-validation 14/14 PASS** на локальных камерах; остаток — ONVIF backlog P0 + GO/NO-GO |

---

## Порт ожидания A (внешние зависимости — НЕ в основном пути)

> Эти задачи не имеют программного остатка на стороне репозитория (блокеры среды). В основной конвейер не включаются; после появления ресурса — единичный шаг, не требующий возврата к коду.

| Код | Блокер | Задача | Как только появится ресурс |
|---|---|---|---|
| A1 | Реальные камеры 192.168.10.0/24 + сервер на localhost:8080 | Этап 8: `field-validation.ps1` (ONVIF/RTSP/HLS), ONVIF backlog P0 (WS e2e без refresh, latency-gate), GO/NO-GO по `PLAN_RELEASE.md` | Прогнать 8.1→8.2→8.3, зафиксировать протокол |
| A2 | macOS + Xcode | iOS: сборка 5 экранов (2.1), iOS-трек функционала (UPnPDiscovery, APNs) | Ветка `platform-client-ios`, сборка, что отсутствует — дополнить |
| A3 | Firebase Console (внешний) | `google-services.json` + активация FCM push (2.2) | Положить файл по пути, условный плагин подхватит; пересобрать APK |
| A4 | Linux-хост с bash/tar/gzip/docker | NAS-пакеты SPK/QPKG/APK (7.5) | `./scripts/build-nas-packages.sh all` на Linux |
---

## ОСНОВНОЙ ПОСЛЕДОВАТЕЛЬНЫЙ ПЛАН (выполняется один раз, без возвратов)

### Фаза 0 — Синхронизация репозитория (5 мин)
| # | Задача | Выход |
|---|---|---|
| 0.1 | Push 2 незапушенных коммитов в `github/main`: `git push github main` | `main` = `github/main`, удалённая чистота |

> ⚠️ Нельзя переходить к фазе 1 до push: иначе последующие коммиты ещё сильнее разойдут ветки.

### Фаза 1 — Подъём локальных зелёных гейтов до состояния «релиз-готовности» (0.5–1 день)
> Логика: всё, что можно зафиксировать **без внешних ресурсов**, закрыть сейчас — тогда Фаза 3 не будет иметь возвратов к коду.

| # | Задача | Выход-гейт |
|---|---|---|
| 1.1 | `verify-kmp-phase1.py` полный режим (3.3) — `python scripts/ci/verify-kmp-phase1.py` + `check-*` (python+gradle+native) | 100% зелёный; зафиксировать в `REMAINING_TASKS` |
| 1.2 | Подключить `integrationTest` (PostgreSQL/Redis) строгим гейтом в `ci.yml` (3.4) — проверить, что `docker-compose.integration.yml` services подняты и `:server:api:integrationTest` без `\|\| true` | Зелёный CI-гейт (проверить из workflow, т.к. локально уже ✅) |
| 1.3 | Ratchet-вал на лимиты Detekt/KtLint: полный прогон `detekt --rerun-tasks`, `ktlintCheck`; **не понижать** существующие пороги | 0 регрессий; пороги монотонны |
| 1.4 | Полная сверка `.gitignore` с фактически трекаемыми файлами (техдолг №1: нативные `*.a/*.dll/CMakeCache/build.ninja/*.obj/*.vcxproj`, `**/build/**`, `.tmphome/`, `diagnostics/`, `logs/`) | `git ls-files` содержит только исходники; чистое дерево |
| 1.5 | Деконсолидация остатков `TODO*`/`TASK*`/`SPRINT_*`/`PHASE1_*` в `docs/planning/` — классифицировать: закрыть/пометить/учесть; `PLAN_TECH_DEBT_TODO.md` привести к факту (закрытые Этапы 3,5) | Единый `REMAINING_TASKS.md` — единственный программный трекер; 0 дублей-todo |

### Фаза 2 — Доработки функционала, доступные без внешних ресурсов (1–2 дня)
| # | Задача | Выход-гейт |
|---|---|---|
| 2.1 | **AI-агент до рабочего состояния (P2)** — `ai-agent/`: LLM-интеграция (LM Studio/Ollama готовы в `.env.example`), инструменты камер (`IPCameraTools.py`), авторизация (**уже вынесена из кода** — дoвести до полного сценария через реальный сервер), тесты `pytest` | `ai-agent/tests` зелёные; запуск FastAPI без ошибок |
| 2.2 | **Неблокирующие исправления из стаб-аудита** `docs/stub-audit-active-2026-08-09.md`: WebRTC placeholder SDP → реальный/явный fallback; LDAP-контур доводка | Отчёт обновлён; SDP-заглушка закрыта |
| 2.3 | **Опциональный cinterop Live555/FFmpeg** (`-Pipcss.enableNativeInterop=true`) — проверить флаг и graceful fallback; контракты `GATE` из `config/` | Флаг работает без хард-фейла |
| 2.4 | **Кластеризация/облако — доводка кода под прод** (Фаза 3 `PLAN_ROADMAP`): S3-sync/репликация/health-check — прогон unit+integration (mock-контур) | `:shared:desktopTest` + `:server:api:test` зелёные |

### Фаза 3 — Финальные сборки и CI, независимые от внешних ресурсов (0.5–1 день)
| # | Задача | Выход-гейт |
|---|---|---|
| 3.1 | Пересборка серверного/desktop/android артефактов после правок Фаз 1–2: `:server:api:installDist`, `:platforms:client-desktop-x86_64:app:packageDistributionForCurrentOS`, `:android:app:assembleDebug` | Артефакты актуальной ревизии (MSI/APK/JAR) |
| 3.2 | Прогон `Trivy`(container-scan) + `secret-scan` + `link-check` (docs-link-check-active-scope) локально | 0 high; 0 битых ссылок; 0 утечек секретов |
| 3.3 | Включить в `ci.yml` строгие гейты для новых python/KMP/check шагов (синхр. с 1.1–1.3) | CI-конфиг отражает реальные таски |
---
## 📌 Ход выполнения (журнал, 14.09.2026)
> Правило «не возвращаемся»: выполненные шаги фиксируются здесь. Внешне-зависимые остатки помечены портом A и не блокируют основной путь.

| Шаг | Результат | Коммит |
|---|---|---|
| **Фаза 0.1** | Push `main` → `github/main` (sync 0↔0) | `1cf8cbcf..push` |
| **Ревизия 15.09** | Синхронизация планов с фактом: закрыто 9 расхождений (Этап 4 Android-дубль ✅, Этап 8 RTSP 14/14 PASS, Этап 6/V2 npm 22→0 + dependabot, Kover-пороги 30/25/20, detekt-гейт верифицирован) | `f027220d` |
| **Ревизия 15.09** | Секреты NAS выведены из 16 трекаемых документов + pre-commit guard (`.githooks/`); рабочее дерево чистое | `2ac6cf17` |
| **Фаза 1.1** | `verify-kmp-phase1.py` полный режим (5 python + 3 gradle) — 100% зелёный | `build-logs/kmp-phase1-full.json` |
| **Фаза 1.3** | `detekt` зелёный, порог 4600 не превышен | — |
| **Фаза 1.4** | Выведены IDE-кэши `.vs/` (23 файла), защищены `.vsidx/.wsuo` и `build-logs/` | `bbc64756` |
| **Фаза 1.5** | `PLAN_TECH_DEBT_TODO.md`, `REMAINING_TASKS.md` актуализированы; `docs/planning` → пост-релиз (Этап 10) | `5fcaa633`, `4cd93f5e` |
| **Фаза 2.1** | AI-агент: починен `pytest.ini` (дубль `addopts` + `pythonpath=src`), испорченный `memory/__init__.py`, порядок `bool` в `_detect_value_type`; **46 passed / 3 skipped** | `7b8856dc` |
| **Фаза 2.1b** | AI-агент green-gate: исправлен `test_heartbeat` (async-context mock), `test_tool_execution_flow` (safe success-key), e2e вынесены в `-m e2e` (внешние сервисы, порт A). **Обычный pytest: 42 passed / 2 skipped / 10 deselected — ЗЕЛЁНЫЙ (exit 0)** | `5a74c4e0` |
| **Фаза 3.1** | `:server:api:installDist` — BUILD SUCCESSFUL (22s) | — |
| **Фаза 3.2** | link-check: 1119 md / 2286 ссылок / **0 битых**; секреты выведены из git (`SECRETS_ROTATION.md` → шаблон, реальные в local); Trivy — CI-гейт (нет локально) | `5dfcd678` |
| **Фаза 3.3** | CI ужесточён: Android-гейт строгий (SDK настроен), полный набор KMP python-гейтов в CI | `2db58170` |
| **Фаза 3.4** | `:server:api:test` — BUILD SUCCESSFUL (2м4с). `:shared:desktopTest` — BUILD SUCCESSFUL (18s). Ключевые гейты продукта зелёные. | — |

**Фаза 2 — остаток (требует внешних ресурсов/подтверждения, порты A):**
- AI-агент: обычный pytest **зелёный** (42 passed / 2 skipped, exit 0). Остаток — e2e (10 тестов, `-m e2e`: реальный ChromaDB :8000 / LLM / сервер, порт A) и реальная интеграция с Ktor backend (нет сервера, порт A).
- 2.2 `OfferSdpAnswerFactory` — **не заглушка**: рабочий SDP-answer из offer (по коду), остаётся только style-замечание detekt (не трогаем — без риска регрессии). LDAP-доводка — порт A (нет LDAP-сервера).
- 2.3 cinterop — требуется нативная пересборка (Linux/macOS), порт A.
- 2.4 кластеризация — прогон `:shared:desktopTest` + `:server:api:test` отложен к Фазе 3.1 (пересборка).
### Фаза 4 — Полевая валидация и GO/NO-GO (только при появлении камер, порт A1)
| # | Задача | Выход-гейт |
|---|---|---|
| 4.1 | `field-validation.ps1` на реальных камерах + ONVIF backlog P0 | Протокол полевой валидации |
| 4.2 | GO/NO-GO чеклист (`PLAN_RELEASE.md` п.2) — все гейты зелёные: build/KMP/Trivy/link-check/stub-блокеры | Подписанный GO/NO-GO |

### Фаза 5 — Релиз 0.6.0-beta (0.5–1 день)
| # | Задача | Выход-гейт |
|---|---|---|
| 5.1 | `CHANGELOG` + `gradle.properties` версия `0.6.0-beta`; `release.yml` автовалидация | Автовалидация проходит |
| 5.2 | GitHub Secrets (`DOCKER_USERNAME`/`DOCKER_PASSWORD`/`GH_TOKEN`) — настроить в репозитории | `release.yml` / `cd.yml` могут публиковать |
| 5.3 | `git tag v0.6.0-beta && git push github v0.6.0-beta`; проверить GitHub Release + Docker `ghcr.io/rekadzeav/ip-css` | Релиз опубликован, артефакты скачиваемы, Docker-образ в ghcr.io |

### Фаза 6 — Пост-релизная волна к v1.0.0 (Этап 10, НЕ пересекается с Фазами 1–5)
| # | Задача | Выход-гейт |
|---|---|---|
| 6.1 | Унификация Model/DTO и репозиториев Shared↔Server (единый источник моделей, устранение дублей мапперов) — «заморозка релиза» снята | Сборка зелёная после унификации |
| 6.2 | Ratchet-завершение: detekt → 0-ориентированный (сейчас 4600), Kover +5% волной | Пороги монотонно растут, гейты зелёные |
| 6.3 | HA/мониторинг/E2E на прод-контуре (Фаза 6 `PLAN_ROADMAP`): Redis/LB, мониторинг | E2E-прогоны зелёные |
| 6.4 | Консолидация planning-документов: `COMPREHENSIVE_DEVELOPMENT_PLAN_V2.md`, `DEVELOPMENT_ROADMAP.md`, `ai-agent/PROJECT_PROMPT.md`, `POETAPNYJ_PLAN_REALIZACII.md` | Один стратегический документ, 0 дублей |
| 6.5 | Ревизия гайдов-дублей: `NETWORK_SCANNER*`, `ONVIF_EVENT*`, `DEPLOYMENT_GUIDE_V2` vs `versions/`, `API_V2` vs OpenAPI | 0 дублей; ссылки целы |
| 6.6 | RC-цикл → **v1.0.0 (Q4 2026)** | Релиз v1.0.0 |

---

## Матрица «не возвращаемся» (защита от повторов)

| Когда было риском | Где закрыто этим планом |
|---|---|
| GitHub отстаёт на 2 коммита | Фаза 0 (0.1) — push первым шагом |
| Внешние блокеры блокировали конвейер | Порт ожидания A1–A4 — изоляция: программный конвейер не ждёт аппаратуру/macOS/Firebase/NAS |
| Документация расходится с кодом | Документация обновляется внутри каждого шага |
| Ветки расходятся без PR | Только `main`, каждая веха = коммит + push |
| Дубли TODO/планов | Фаза 1.5 — деконсолидация до релиза; `REMAINING_TASKS.md` — единственный трекер |

## Оценка трудозатрат (программная часть, без портов ожидания A1–A4)

| Фаза | Оценка |
|---|---|
| Фаза 0 | 5 мин |
| Фаза 1 | 0.5–1 день |
| Фаза 2 | 1–2 дня |
| Фаза 3 | 0.5–1 день |
| Фаза 5 | 0.5–1 день (после Фазы 4 / порта A1 при необходимости GO) |
| Фаза 6 | 1–2 дня (пост-релиз, параллелизуемо) |
| **Итого программно** | **~3–5 рабочих дней** + календарные ожидания (камеры, релизный CI, Firebase/A2–A4) |