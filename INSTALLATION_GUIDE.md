# Руководство по установке компонентов для сборки IP-CSS

**Версия документации:** 2.0
**Дата последнего обновления:** Январь 2025
**Версия проекта:** Alfa-0.0.1

---

## Быстрый старт

### Автоматическая установка через Chocolatey (рекомендуется)

1. **Запустите PowerShell от имени администратора**

2. **Установите Chocolatey** (если не установлен):
```powershell
Set-ExecutionPolicy Bypass -Scope Process -Force
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
```

3. **Установите все необходимые компоненты:**
```powershell
choco install openjdk17 nodejs-lts cmake ffmpeg -y
```

4. **Перезапустите терминал** для применения изменений PATH

5. **Проверьте установку:**
```powershell
.\scripts\check-dependencies.ps1
```

---

## Общие требования (для всех модулей)

### Критически важные (P0)

1. **JDK 17+** (Java Development Kit)
   - Требуется для: всех Kotlin/JVM модулей
   - Версия: JDK 17 (LTS) или выше
   - **Проверка:** `java -version`, `javac -version`

2. **Gradle 8.4+**
   - Требуется для: всех Gradle модулей
   - Включен в проект через Gradle Wrapper (`gradlew`, `gradlew.bat`)
   - **Проверка:** `./gradlew --version`

3. **Kotlin 2.0.21**
   - Устанавливается автоматически через Gradle plugins
   - Версия указана в `build.gradle.kts`

---

## Детальные инструкции по установке

### 1. Java JDK 17+

**Вариант 1: Через Chocolatey**
```powershell
choco install openjdk17 -y
```

**Вариант 2: Ручная установка**
- **Скачать**: https://adoptium.net/
- **Выбрать**: OpenJDK 17 (LTS)
- **Проверка**: `java -version` и `javac -version`

### 2. Node.js 20+

**Вариант 1: Через Chocolatey**
```powershell
choco install nodejs-lts -y
```

**Вариант 2: Ручная установка**
- **Скачать**: https://nodejs.org/
- **Выбрать**: LTS версию (20.x или выше)
- **Проверка**: `node --version` и `npm --version`

### 3. CMake 3.15+

**Вариант 1: Через Chocolatey**
```powershell
choco install cmake -y
```

**Вариант 2: Ручная установка**
- **Скачать**: https://cmake.org/download/
- **Установить**: Windows x64 Installer
- **Важно**: Выберите опцию "Add CMake to system PATH"
- **Проверка**: `cmake --version`

### 4. FFmpeg

**Вариант 1: Через Chocolatey**
```powershell
choco install ffmpeg -y
```

**Вариант 2: Ручная установка**
- **Скачать**: https://ffmpeg.org/download.html
- **Примечание**: Для сборки нативных библиотек нужны dev библиотеки (libavformat-dev, libavcodec-dev, libavutil-dev, libswscale-dev, libswresample-dev)
- На Windows рекомендуется использовать vcpkg или скачать dev версию отдельно
- **Проверка**: `ffmpeg -version`

### 5. Android SDK (только для сборки Android приложения)

**Вариант 1: Android Studio**
- **Установить**: https://developer.android.com/studio
- Android SDK установится автоматически

**Вариант 2: Command-line Tools**
- **Скачать**: https://developer.android.com/studio#command-tools
- **Настроить переменную окружения**:
  ```powershell
  [System.Environment]::SetEnvironmentVariable("ANDROID_HOME", "C:\Users\$env:USERNAME\AppData\Local\Android\Sdk", "User")
  ```
- **Установить компоненты**:
  ```powershell
  $env:ANDROID_HOME\cmdline-tools\latest\bin\sdkmanager.bat "platforms;android-34" "build-tools;34.0.0" "platform-tools"
  ```

### 6. C++ компилятор (только для нативных библиотек)

**Вариант 1: Visual Studio 2022**
- Установите Visual Studio 2022 с компонентами "Desktop development with C++"

**Вариант 2: MinGW-w64**
```powershell
choco install mingw -y
```

**Проверка**: `g++ --version` или наличие Visual Studio

---

## Требования по модулям

### Минимальная сборка (без нативных библиотек и веб-интерфейса)

Если вам не нужны нативные библиотеки и веб-интерфейс, вы можете собрать только Kotlin модули:

**Требуется:**
- ✅ JDK 17+
- ✅ Gradle 8.4+ (включен в проект)

**Команды сборки:**
```powershell
.\gradlew.bat :core:common:build
.\gradlew.bat :core:network:build
.\gradlew.bat :shared:build
.\gradlew.bat :server:api:build
```

### Полная сборка

Для полной сборки всех компонентов потребуются все перечисленные выше инструменты:

**Требуется:**
- ✅ JDK 17+
- ✅ Gradle 8.4+
- ✅ Node.js 20+ (для веб-интерфейса)
- ✅ CMake 3.15+ (для нативных библиотек)
- ✅ FFmpeg (для нативных библиотек)
- ✅ C++ компилятор (для нативных библиотек)
- ⚠️ Android SDK (для Android приложения)

---

## Проверка установки

### Автоматическая проверка

Запустите скрипт проверки:
```powershell
.\scripts\check-dependencies.ps1
```

Или скрипт установки (требует прав администратора):
```powershell
.\scripts\install-build-dependencies.ps1
```

### Ручная проверка

Проверьте каждый компонент вручную:
```powershell
java -version
javac -version
node --version
npm --version
cmake --version
ffmpeg -version
g++ --version  # или проверьте наличие Visual Studio
```

---

## Текущий статус установки

После запуска скрипта проверки вы увидите статус всех компонентов.

### ✅ Обычно уже установлено:
- Java 19+ (JDK доступен)
- Gradle Wrapper (gradlew.bat найден в проекте)

### ❌ Обычно требуется установить:
1. **Node.js 20+** - для сборки веб-интерфейса (server/web)
2. **CMake 3.15+** - для сборки нативных библиотек (native/)
3. **FFmpeg** - для нативных библиотек (опционально, но рекомендуется)
4. **C++ компилятор** - для нативных библиотек (опционально)
5. **Android SDK** - для сборки Android приложения (опционально)

---

## Следующие шаги

1. **Перезапустите терминал** после установки новых компонентов
2. **Проверьте установку**: `.\scripts\check-dependencies.ps1`
3. **Начните сборку проекта** согласно документации:
   - Для минимальной сборки: `.\gradlew.bat build`
   - Для полной сборки: см. [BUILD_NATIVE_LIBRARY.md](BUILD_NATIVE_LIBRARY.md)

---

## Созданные файлы и скрипты

1. **scripts/check-dependencies.ps1** - скрипт проверки всех зависимостей
2. **scripts/install-build-dependencies.ps1** - скрипт проверки и установки (требует прав администратора)
3. **scripts/quick-install.ps1** - быстрая установка через Chocolatey (требует прав администратора)

---

## Дополнительная информация

- **Подробные требования по модулям**: См. [LOCAL_BUILD_REQUIREMENTS.md](LOCAL_BUILD_REQUIREMENTS.md) для детальной информации о требованиях каждого модуля
- **Сборка нативных библиотек**: См. [BUILD_NATIVE_LIBRARY.md](BUILD_NATIVE_LIBRARY.md)
- **Архитектура проекта**: См. [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

---

**Последнее обновление:** Январь 2025

> **📝 Примечание:** Этот документ объединяет информацию из INSTALL_INSTRUCTIONS.md, INSTALLATION_SUMMARY.md, QUICK_INSTALL.md и LOCAL_BUILD_REQUIREMENTS.md для удобства навигации.

