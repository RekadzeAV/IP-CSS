# IP Camera Surveillance System - Web Interface

Веб-интерфейс для системы видеонаблюдения на базе Next.js, React, Material-UI и Redux Toolkit.

## Технологии

- **Next.js 14** - React фреймворк с App Router
- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Material-UI (MUI)** - компоненты UI
- **Redux Toolkit** - управление состоянием
- **Axios** - HTTP клиент
- **Socket.IO Client** - WebSocket клиент
- **Notistack** - уведомления

## Структура проекта

```
server/web/
├── src/
│   ├── app/                  # Next.js App Router страницы
│   │   ├── layout.tsx        # Корневой layout
│   │   ├── page.tsx          # Главная страница (редирект)
│   │   ├── login/            # Страница входа
│   │   ├── dashboard/        # Главная панель
│   │   ├── cameras/          # Страницы камер
│   │   ├── events/           # Страница событий
│   │   ├── recordings/       # Страница записей
│   │   └── settings/         # Страница настроек
│   ├── components/           # React компоненты
│   │   ├── Layout/           # Layout компонент
│   │   ├── CameraCard/       # Карточка камеры
│   │   └── VideoPlayer/      # Видеоплеер
│   ├── store/                # Redux store
│   │   ├── slices/           # Redux slices
│   │   ├── index.ts          # Store конфигурация
│   │   └── hooks.ts          # Typed hooks
│   ├── services/             # API сервисы
│   │   ├── authService.ts    # Сервис аутентификации
│   │   └── cameraService.ts  # Сервис камер
│   ├── types/                # TypeScript типы
│   │   └── index.ts          # Все типы данных
│   └── utils/                # Утилиты
│       └── api.ts            # Axios конфигурация
├── package.json
├── tsconfig.json
└── next.config.js
```

## Установка

```bash
cd server/web
npm install
```

## Настройка

Создайте файл `.env.local` на основе `.env.local.example`:

```bash
cp .env.local.example .env.local
```

Измените переменные окружения при необходимости:

```env
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
NEXT_PUBLIC_WS_URL=ws://localhost:8080/api/v1/ws
```

## Запуск

### Режим разработки

```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Сборка для production

```bash
npm run build
npm start
```

## Основные функции

### ✅ Реализовано

- ✅ Аутентификация (вход/выход)
- ✅ Главная панель с статистикой
- ✅ Список камер
- ✅ Детали камеры
- ✅ Добавление/удаление камер
- ✅ Тестирование подключения к камере
- ✅ Базовая навигация
- ✅ Redux store для управления состоянием
- ✅ API интеграция

### 🚧 В разработке

- 🚧 Видеоплеер (требует интеграции с медиа-сервером)
- 🚧 Страница событий
- 🚧 Страница записей
- 🚧 Страница настроек
- 🚧 WebSocket интеграция для real-time обновлений
- 🚧 Обнаружение камер в сети

## API Endpoints

Веб-интерфейс использует следующие API endpoints:

- `GET /api/v1/cameras` - список камер
- `GET /api/v1/cameras/{id}` - детали камеры
- `POST /api/v1/cameras` - создание камеры
- `PUT /api/v1/cameras/{id}` - обновление камеры
- `DELETE /api/v1/cameras/{id}` - удаление камеры
- `POST /api/v1/cameras/{id}/test` - тест подключения
- `GET /api/v1/cameras/discover` - обнаружение камер
- `POST /api/v1/auth/login` - вход
- `POST /api/v1/auth/logout` - выход
- `GET /api/v1/users/me` - текущий пользователь

## Разработка

### Добавление новых компонентов

Компоненты размещаются в `src/components/` с следующей структурой:

```
components/
  ComponentName/
    ComponentName.tsx
    index.ts
    ComponentName.test.tsx (опционально)
```

### Добавление новых страниц

Страницы создаются в `src/app/` используя Next.js App Router.

### Работа с Redux

Используйте typed hooks из `src/store/hooks.ts`:

```typescript
import { useAppDispatch, useAppSelector } from '@/store/hooks';

// В компоненте
const dispatch = useAppDispatch();
const cameras = useAppSelector((state) => state.cameras.cameras);
```

### API сервисы

API сервисы размещаются в `src/services/` и используют настроенный axios клиент из `src/utils/api.ts`.

## Лицензия

См. основной файл LICENSE в корне проекта.

